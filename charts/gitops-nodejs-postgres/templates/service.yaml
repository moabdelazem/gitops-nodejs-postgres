apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-nodejs-postgres.fullname" . }}
  labels:
    {{- include "gitops-nodejs-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-nodejs-postgres.selectorLabels" . | nindent 4 }}
